# Archived agent code

## `muliatgent1.py`

Typo duplicate of `agent/multiagent.py` (three-lane planner/worker/critic pipeline). It was local WIP, never registered in `agent/registry.py`, and was listed in `.gitignore`.

**Use:** `agent/multiagent.py` via `run_strategy("multiagent", ...)`.

Archived 2026-05-18. Safe to delete this folder if you no longer need the diff reference.
